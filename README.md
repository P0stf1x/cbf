# Compiler for BrainFuck

Interpreter for Brainfuck written in C++. Eventually it will also support Compilation/JIT compilation

## Usage

```sh
./cbf ./path/to/bf/source
```

```sh
./cbf --help
```

## Compiling

```sh
mkdir build
cd build
cmake ..
cmake --build .
```
